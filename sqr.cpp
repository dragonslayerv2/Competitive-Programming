#include<iostream>
using namespace std;

long long sqrt(long long n)
{
	long long llimit=0;
	long long ulimit=n;
	while(ulimit>=llimit)
	{
		long long mlimit=getmid(llimit,ulimit);
		if(mlimit*mlimit
	}
}
