#include<iostream>
using namespace std;

struct node
{
	bool is_leaf;
	vector<node *> character;
};

node *root=NULL;


void insert_trie(node *root,char *data)
{
	for(int i=0;data[i]!=NULL;i++)
	{
	
	}
}
